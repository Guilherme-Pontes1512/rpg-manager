spring:
  config:
    activate:
      on-profile: postgres

  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/rpg_manager}
    username: ${POSTGRES_USER:rpg_manager}
    password: ${POSTGRES_PASSWORD:rpg_manager}

  jpa:
    hibernate:
      ddl-auto: none
