spring:
  config:
    activate:
      on-profile: dev
  security:
    user:
      name: admin
      password: admin
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.sql: DEBUG

vaadin:
  launch-browser: true

app:
  email:
    enabled: true
